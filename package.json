{
  "name": "cloudflare_cable",
  "version": "2.0.0",
  "description": "FIDO caBLE Tunnel Server using Cloudflare Durable Objects (SQLite Backend)",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.0.0",
    "wrangler": "^4.0.0"
  }
}